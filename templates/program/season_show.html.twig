{% extends 'base.html.twig' %}

{% block title %}{{ program.title }} Saison #{{ season.number }} {% endblock %}

{% block body %}

<div class="media">
    <div>
        {% for episode in episodes %} <br>
            Episode : <a href="{{path('program_episode_show',{seasonId:season.id,programId:program.id,episodeId:episode.id})}}">{{episode.number}}</a> <br>
            Titre : {{ episode.title }} <br>
            Synopsis : {{ episode.synopsis }} <br>
        {% endfor %}
    </div>
</div>
<br>
<a href="{{ path('program_index') }}">
    Retour à la liste des programmes
</a>

{% endblock %}